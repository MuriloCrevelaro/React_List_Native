import { StyleSheet } from "react-native";

export const TaskListStyles = StyleSheet.create({
    taskListContainer: {
        flex: 1,
        borderWidth: 3, 
        borderStyle: "solid",
        borderColor: "white",
    }
})